<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Zinara Digital - Unleash Your Digital Potential</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --color-deep-teal: #00303F;
            --color-blush: #EDDFE0;
            --color-sea-blue: #006B7D;
            --color-orange: #FF6B35;
            --color-aqua: #03A6A1;
            --color-gray-accent: #7A7A7A;
            --color-white: #FFFFFF;
            --color-light: #F8F9FA;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            line-height: 1.6;
            color: var(--color-deep-teal);
            overflow-x: hidden;
        }

        /* Navigation */
        nav {
            background: var(--color-white);
            padding: 1.2rem 5%;
            position: fixed;
            width: 100%;
            top: 0;
            z-index: 1000;
            box-shadow: 0 2px 15px rgba(0,0,0,0.1);
            transition: all 0.3s ease;
        }

        nav.scrolled {
            padding: 0.8rem 5%;
            background: rgba(255, 255, 255, 0.98);
            backdrop-filter: blur(10px);
        }

        .nav-container {
            max-width: 1400px;
            margin: 0 auto;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo-container {
            display: flex;
            align-items: center;
            gap: 1rem;
        }

        .logo-image {
            height: 50px;
            width: auto;
            transition: transform 0.3s ease;
        }

        .logo-image:hover {
            transform: scale(1.05);
        }

        .logo-text {
            font-size: 1.5rem;
            font-weight: bold;
            color: var(--color-deep-teal);
            text-decoration: none;
        }

        .nav-menu {
            display: flex;
            list-style: none;
            gap: 2.5rem;
            align-items: center;
        }

        .nav-menu a {
            text-decoration: none;
            color: var(--color-deep-teal);
            font-weight: 500;
            transition: color 0.3s ease;
            position: relative;
            font-size: 1rem;
        }

        .nav-menu a:hover {
            color: var(--color-orange);
        }

        .nav-menu a::after {
            content: '';
            position: absolute;
            bottom: -5px;
            left: 0;
            width: 0;
            height: 2px;
            background: var(--color-orange);
            transition: width 0.3s ease;
        }

        .nav-menu a:hover::after {
            width: 100%;
        }

        .hamburger {
            display: none;
            flex-direction: column;
            cursor: pointer;
            gap: 5px;
        }

        .hamburger span {
            width: 28px;
            height: 3px;
            background: var(--color-deep-teal);
            transition: all 0.3s ease;
            border-radius: 2px;
        }

        /* Hero Section */
        .hero {
            margin-top: 80px;
            min-height: 95vh;
            display: flex;
            align-items: center;
            background: linear-gradient(135deg, var(--color-blush) 0%, var(--color-white) 100%);
            position: relative;
            overflow: hidden;
        }

        .hero::before {
            content: '';
            position: absolute;
            top: -30%;
            right: -15%;
            width: 70%;
            height: 130%;
            background: linear-gradient(135deg, var(--color-aqua) 0%, transparent 70%);
            opacity: 0.08;
            border-radius: 50%;
            animation: float 20s ease-in-out infinite;
        }

        @keyframes float {
            0%, 100% { transform: translateY(0) rotate(0deg); }
            50% { transform: translateY(-30px) rotate(5deg); }
        }

        .hero-container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 0 5%;
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 5rem;
            align-items: center;
        }

        .hero-content {
            z-index: 2;
        }

        .hero-logo {
            width: 120px;
            margin-bottom: 1.5rem;
            animation: fadeInUp 1s ease;
        }

        .hero-content h1 {
            font-size: 3.8rem;
            line-height: 1.15;
            margin-bottom: 1.5rem;
            color: var(--color-deep-teal);
            font-weight: 700;
            animation: fadeInUp 1s ease 0.2s both;
        }

        .hero-subtitle {
            font-size: 1.4rem;
            color: var(--color-sea-blue);
            margin-bottom: 1rem;
            font-weight: 600;
            animation: fadeInUp 1s ease 0.3s both;
        }

        .hero-content p {
            font-size: 1.2rem;
            color: var(--color-gray-accent);
            margin-bottom: 2.5rem;
            line-height: 1.8;
            animation: fadeInUp 1s ease 0.4s both;
        }

        .tagline {
            font-size: 1.3rem;
            color: var(--color-aqua);
            font-style: italic;
            margin-bottom: 2rem;
            animation: fadeInUp 1s ease 0.5s both;
        }

        .cta-button {
            display: inline-block;
            padding: 1.1rem 3rem;
            background: var(--color-orange);
            color: var(--color-white);
            text-decoration: none;
            border-radius: 50px;
            font-weight: 600;
            font-size: 1.1rem;
            transition: all 0.3s ease;
            box-shadow: 0 8px 20px rgba(255, 107, 53, 0.3);
            animation: fadeInUp 1s ease 0.6s both;
            border: none;
            cursor: pointer;
        }

        .cta-button:hover {
            background: var(--color-sea-blue);
            transform: translateY(-3px);
            box-shadow: 0 12px 30px rgba(0, 107, 125, 0.4);
        }

        .hero-image {
            position: relative;
            z-index: 2;
            animation: fadeInRight 1s ease;
        }

        .hero-image img {
            width: 100%;
            height: auto;
            border-radius: 20px;
            box-shadow: 0 25px 70px rgba(0, 48, 63, 0.2);
        }

        /* Partner Section */
        .partner-section {
            background: linear-gradient(135deg, var(--color-blush) 0%, #F5E8E9 100%);
            padding: 6rem 5%;
            text-align: center;
        }

        .partner-section h2 {
            font-size: 3rem;
            color: var(--color-deep-teal);
            margin-bottom: 1.5rem;
            font-weight: 700;
        }

        .partner-section .subtitle {
            font-size: 1.5rem;
            color: var(--color-sea-blue);
            margin-bottom: 2rem;
            font-weight: 600;
        }

        .partner-section p {
            font-size: 1.2rem;
            max-width: 900px;
            margin: 0 auto 2.5rem;
            color: var(--color-gray-accent);
            line-height: 1.9;
        }

        .tagline-emphasis {
            font-size: 1.3rem;
            color: var(--color-aqua);
            font-style: italic;
            font-weight: 600;
            margin-top: 2rem;
        }

        /* Process Steps Section */
        .process-section {
            padding: 7rem 5%;
            background: var(--color-white);
        }

        .process-container {
            max-width: 1400px;
            margin: 0 auto;
        }

        .process-header {
            text-align: center;
            margin-bottom: 5rem;
        }

        .process-header h2 {
            font-size: 3rem;
            color: var(--color-deep-teal);
            margin-bottom: 1rem;
            font-weight: 700;
        }

        .process-header p {
            font-size: 1.3rem;
            color: var(--color-gray-accent);
        }

        .process-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
            gap: 3.5rem;
            margin-top: 4rem;
        }

        .process-card {
            background: var(--color-white);
            padding: 3rem;
            border-radius: 20px;
            text-align: center;
            box-shadow: 0 10px 40px rgba(0, 48, 63, 0.08);
            transition: all 0.4s ease;
            position: relative;
            border: 2px solid var(--color-blush);
        }

        .process-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 6px;
            background: linear-gradient(90deg, var(--color-orange), var(--color-aqua));
            border-radius: 20px 20px 0 0;
        }

        .process-card:hover {
            transform: translateY(-15px);
            box-shadow: 0 20px 50px rgba(0, 107, 125, 0.15);
            border-color: var(--color-aqua);
        }

        .process-number {
            width: 90px;
            height: 90px;
            background: linear-gradient(135deg, var(--color-orange), #FF8C5A);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 2rem;
            font-size: 2.5rem;
            color: var(--color-white);
            font-weight: bold;
            box-shadow: 0 8px 20px rgba(255, 107, 53, 0.3);
        }

        .process-card h3 {
            font-size: 1.8rem;
            color: var(--color-deep-teal);
            margin-bottom: 1.5rem;
            font-weight: 700;
        }

        .process-card p {
            color: var(--color-gray-accent);
            line-height: 1.9;
            font-size: 1.1rem;
        }

        .process-card .timeline {
            margin-top: 1rem;
            font-style: italic;
            color: var(--color-aqua);
            font-weight: 600;
        }

        /* About Section */
        .about-section {
            padding: 7rem 5%;
            background: linear-gradient(135deg, var(--color-deep-teal) 0%, var(--color-sea-blue) 100%);
            color: var(--color-white);
            text-align: center;
        }

        .about-container {
            max-width: 1200px;
            margin: 0 auto;
        }

        .about-label {
            font-size: 1.1rem;
            text-transform: uppercase;
            letter-spacing: 2px;
            color: var(--color-orange);
            margin-bottom: 1rem;
            font-weight: 600;
        }

        .about-section h2 {
            font-size: 2.8rem;
            margin-bottom: 2rem;
            font-weight: 700;
        }

        .about-section p {
            font-size: 1.2rem;
            line-height: 1.9;
            margin-bottom: 3rem;
        }

        .mission-vision {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 3rem;
            margin-top: 4rem;
        }

        .mission-card, .vision-card {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            padding: 3rem;
            border-radius: 20px;
            border: 2px solid rgba(255, 255, 255, 0.2);
            transition: all 0.3s ease;
        }

        .mission-card:hover, .vision-card:hover {
            background: rgba(255, 255, 255, 0.15);
            transform: translateY(-10px);
        }

        .mission-card h3, .vision-card h3 {
            font-size: 1.8rem;
            color: var(--color-orange);
            margin-bottom: 1.5rem;
            text-transform: uppercase;
            font-weight: 700;
        }

        .mission-card p, .vision-card p {
            font-size: 1.1rem;
            line-height: 1.8;
        }

        /* What We Do Section */
        .what-we-do-section {
            padding: 7rem 5%;
            background: var(--color-light);
        }

        .what-we-do-container {
            max-width: 1400px;
            margin: 0 auto;
            text-align: center;
        }

        .what-we-do-container h2 {
            font-size: 3rem;
            color: var(--color-deep-teal);
            margin-bottom: 2rem;
            font-weight: 700;
        }

        .what-we-do-container > p {
            font-size: 1.2rem;
            max-width: 1000px;
            margin: 0 auto 3rem;
            color: var(--color-gray-accent);
            line-height: 1.9;
        }

        .tagline-emphasis-section {
            font-size: 1.4rem;
            color: var(--color-aqua);
            font-style: italic;
            font-weight: 600;
            margin: 3rem 0;
        }

        /* Services Section */
        .services-section {
            padding: 7rem 5%;
            background: var(--color-white);
        }

        .services-container {
            max-width: 1400px;
            margin: 0 auto;
        }

        .services-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(380px, 1fr));
            gap: 3.5rem;
            margin-top: 4rem;
        }

        .service-card {
            background: var(--color-light);
            padding: 3.5rem;
            border-radius: 20px;
            transition: all 0.4s ease;
            border-left: 6px solid var(--color-orange);
            box-shadow: 0 5px 20px rgba(0, 48, 63, 0.08);
        }

        .service-card:hover {
            background: var(--color-blush);
            transform: translateX(15px);
            box-shadow: 0 15px 40px rgba(0, 107, 125, 0.15);
            border-left-color: var(--color-aqua);
        }

        .service-card h3 {
            font-size: 1.9rem;
            color: var(--color-deep-teal);
            margin-bottom: 1.5rem;
            font-weight: 700;
        }

        .service-card p {
            color: var(--color-gray-accent);
            line-height: 1.9;
            margin-bottom: 1.2rem;
            font-size: 1.05rem;
        }

        .service-card .what-you-get {
            margin-top: 2rem;
            padding-top: 2rem;
            border-top: 2px solid var(--color-blush);
        }

        .service-card .what-you-get h4 {
            color: var(--color-orange);
            font-size: 1.2rem;
            margin-bottom: 1rem;
            font-weight: 600;
        }

        .service-card ul {
            list-style: none;
            margin-top: 1.5rem;
        }

        .service-card ul li {
            padding: 0.7rem 0;
            color: var(--color-gray-accent);
            position: relative;
            padding-left: 2rem;
            line-height: 1.7;
        }

        .service-card ul li::before {
            content: '✓';
            position: absolute;
            left: 0;
            color: var(--color-orange);
            font-weight: bold;
            font-size: 1.3rem;
        }

        .service-tags {
            margin-top: 2rem;
            padding-top: 2rem;
            border-top: 2px solid var(--color-blush);
            font-size: 0.95rem;
            color: var(--color-aqua);
            font-weight: 600;
        }

        .all-services-cta {
            text-align: center;
            margin-top: 4rem;
        }

        /* Empowering Growth Section */
        .empowering-section {
            padding: 7rem 5%;
            background: linear-gradient(135deg, var(--color-blush) 0%, var(--color-white) 100%);
        }

        .empowering-container {
            max-width: 1400px;
            margin: 0 auto;
            text-align: center;
        }

        .empowering-container h2 {
            font-size: 3.2rem;
            color: var(--color-orange);
            margin-bottom: 1rem;
            font-weight: 700;
        }

        .empowering-container h3 {
            font-size: 1.8rem;
            color: var(--color-sea-blue);
            margin-bottom: 3rem;
            font-weight: 600;
        }

        .empowering-container p {
            font-size: 1.2rem;
            max-width: 1000px;
            margin: 0 auto 2rem;
            color: var(--color-gray-accent);
            line-height: 1.9;
        }

        /* Testimonials Section */
        .testimonials-section {
            padding: 7rem 5%;
            background: var(--color-white);
        }

        .testimonials-container {
            max-width: 1400px;
            margin: 0 auto;
            text-align: center;
        }

        .testimonials-container h2 {
            font-size: 3rem;
            color: var(--color-deep-teal);
            margin-bottom: 1.5rem;
            font-weight: 700;
        }

        .testimonials-container > p {
            font-size: 1.2rem;
            max-width: 900px;
            margin: 0 auto 4rem;
            color: var(--color-gray-accent);
        }

        .testimonials-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
            gap: 3rem;
            margin-top: 4rem;
        }

        .testimonial-card {
            background: var(--color-light);
            padding: 3rem;
            border-radius: 20px;
            box-shadow: 0 8px 30px rgba(0, 48, 63, 0.08);
            transition: all 0.4s ease;
            text-align: left;
            border-top: 4px solid var(--color-orange);
        }

        .testimonial-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 15px 40px rgba(0, 107, 125, 0.15);
            border-top-color: var(--color-aqua);
        }

        .testimonial-header {
            display: flex;
            align-items: center;
            gap: 1rem;
            margin-bottom: 1.5rem;
        }

        .testimonial-card h4 {
            color: var(--color-deep-teal);
            margin-bottom: 0.3rem;
            font-size: 1.4rem;
            font-weight: 700;
        }

        .testimonial-card h5 {
            color: var(--color-aqua);
            font-size: 1.1rem;
            font-weight: 600;
            margin-bottom: 1.5rem;
        }

        .testimonial-card p {
            color: var(--color-gray-accent);
            line-height: 1.9;
            font-style: italic;
            font-size: 1.05rem;
        }

        /* Portfolio Section */
        .portfolio-section {
            padding: 7rem 5%;
            background: var(--color-light);
        }

        .portfolio-container {
            max-width: 1400px;
            margin: 0 auto;
        }

        .portfolio-header {
            text-align: center;
            margin-bottom: 5rem;
        }

        .portfolio-header h2 {
            font-size: 3rem;
            color: var(--color-deep-teal);
            margin-bottom: 1.5rem;
            font-weight: 700;
        }

        .portfolio-header p {
            font-size: 1.2rem;
            max-width: 900px;
            margin: 0 auto;
            color: var(--color-gray-accent);
        }

        .portfolio-grid {
            display: grid;
            gap: 4rem;
        }

        .portfolio-item {
            background: var(--color-white);
            padding: 4rem;
            border-radius: 20px;
            box-shadow: 0 10px 40px rgba(0, 48, 63, 0.08);
            transition: all 0.4s ease;
        }

        .portfolio-item:hover {
            transform: translateY(-10px);
            box-shadow: 0 20px 50px rgba(0, 107, 125, 0.15);
        }

        .portfolio-item h3 {
            font-size: 2rem;
            color: var(--color-deep-teal);
            margin-bottom: 1rem;
            font-weight: 700;
        }

        .portfolio-item h4 {
            font-size: 1.5rem;
            color: var(--color-orange);
            margin-bottom: 1.5rem;
            font-weight: 600;
        }

        .portfolio-item p {
            color: var(--color-gray-accent);
            line-height: 1.9;
            font-size: 1.1rem;
        }

        /* Stats Section */
        .stats-section {
            padding: 7rem 5%;
            background: linear-gradient(135deg, var(--color-sea-blue) 0%, var(--color-deep-teal) 100%);
            color: var(--color-white);
            text-align: center;
        }

        .stats-container h2 {
            font-size: 3rem;
            margin-bottom: 2rem;
            font-weight: 700;
        }

        .stats-container > p {
            font-size: 1.2rem;
            max-width: 800px;
            margin: 0 auto 4rem;
        }

        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 4rem;
            margin-top: 4rem;
            max-width: 1200px;
            margin-left: auto;
            margin-right: auto;
        }

        .stat-card {
            padding: 2.5rem;
        }

        .stat-number {
            font-size: 5rem;
            font-weight: bold;
            color: var(--color-orange);
            margin-bottom: 1rem;
            line-height: 1;
        }

        .stat-label {
            font-size: 1.5rem;
            margin-bottom: 1rem;
            font-weight: 600;
        }

        .stat-description {
            font-size: 1.05rem;
            opacity: 0.95;
        }

        .process-flow {
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 3rem;
            margin-top: 4rem;
            flex-wrap: wrap;
        }

        .process-step {
            font-size: 1.3rem;
            font-weight: 600;
        }

        .arrow {
            font-size: 2rem;
            color: var(--color-orange);
        }

        /* Contact Section */
        .contact-section {
            padding: 7rem 5%;
            background: var(--color-blush);
        }

        .contact-container {
            max-width: 1400px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 5rem;
            align-items: center;
        }

        .contact-form-wrapper h2 {
            font-size: 3rem;
            color: var(--color-deep-teal);
            margin-bottom: 1.5rem;
            font-weight: 700;
        }

        .contact-form-wrapper > p {
            font-size: 1.1rem;
            color: var(--color-gray-accent);
            margin-bottom: 2.5rem;
        }

        .contact-form {
            display: flex;
            flex-direction: column;
            gap: 1.8rem;
        }

        .form-row {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 1.5rem;
        }

        .form-group {
            display: flex;
            flex-direction: column;
        }

        .form-group label {
            margin-bottom: 0.7rem;
            color: var(--color-deep-teal);
            font-weight: 600;
            font-size: 1rem;
        }

        .form-group input,
        .form-group textarea {
            padding: 1.2rem;
            border: 2px solid var(--color-blush);
            border-radius: 10px;
            font-size: 1rem;
            transition: all 0.3s ease;
            font-family: inherit;
            background: var(--color-white);
        }

        .form-group input:focus,
        .form-group textarea:focus {
            outline: none;
            border-color: var(--color-orange);
            box-shadow: 0 0 0 4px rgba(255, 107, 53, 0.1);
        }

        .form-group textarea {
            resize: vertical;
            min-height: 180px;
        }

        .contact-image img {
            width: 100%;
            height: auto;
            border-radius: 20px;
            box-shadow: 0 25px 70px rgba(0, 48, 63, 0.2);
        }

        /* Footer */
        footer {
            background: var(--color-deep-teal);
            color: var(--color-white);
            padding: 5rem 5% 2rem;
        }

        .footer-container {
            max-width: 1400px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: 2fr 1fr 1fr 1fr;
            gap: 4rem;
            margin-bottom: 3rem;
        }

        .footer-brand h3 {
            font-size: 2rem;
            color: var(--color-orange);
            margin-bottom: 1rem;
            font-weight: 700;
        }

        .footer-brand .tagline {
            color: var(--color-aqua);
            font-size: 1.2rem;
            font-style: italic;
            margin-bottom: 1.5rem;
        }

        .footer-brand p {
            margin-bottom: 1rem;
            line-height: 1.8;
        }

        .footer-section h4 {
            font-size: 1.3rem;
            color: var(--color-orange);
            margin-bottom: 1.5rem;
            font-weight: 700;
        }

        .footer-section a {
            color: var(--color-white);
            text-decoration: none;
            display: block;
            margin-bottom: 1rem;
            transition: all 0.3s ease;
            font-size: 1.05rem;
        }

        .footer-section a:hover {
            color: var(--color-orange);
            padding-left: 10px;
        }

        .footer-bottom {
            text-align: center;
            padding-top: 2.5rem;
            border-top: 1px solid rgba(255,255,255,0.1);
            color: var(--color-white);
            font-size: 1rem;
        }

        /* Animations */
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(40px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes fadeInRight {
            from {
                opacity: 0;
                transform: translateX(40px);
            }
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }

        /* Responsive Design */
        @media (max-width: 1200px) {
            .hero-container,
            .contact-container {
                grid-template-columns: 1fr;
                gap: 3rem;
            }

            .footer-container {
                grid-template-columns: repeat(2, 1fr);
            }
        }

        @media (max-width: 768px) {
            .nav-menu {
                position: fixed;
                left: -100%;
                top: 70px;
                flex-direction: column;
                background: var(--color-white);
                width: 100%;
                text-align: center;
                transition: 0.3s;
                box-shadow: 0 10px 27px rgba(0,0,0,0.1);
                padding: 2rem 0;
                gap: 1.5rem;
            }

            .nav-menu.active {
                left: 0;
            }

            .hamburger {
                display: flex;
            }

            .hero-content h1 {
                font-size: 2.5rem;
            }

            .hero-subtitle {
                font-size: 1.2rem;
            }

            .hero-content p {
                font-size: 1.05rem;
            }

            .partner-section h2,
            .process-header h2,
            .about-section h2,
            .what-we-do-container h2,
            .testimonials-container h2,
            .portfolio-header h2,
            .stats-container h2,
            .contact-form-wrapper h2 {
                font-size: 2rem;
            }

            .process-grid,
            .services-grid,
            .stats-grid,
            .testimonials-grid {
                grid-template-columns: 1fr;
            }

            .stat-number {
                font-size: 3.5rem;
            }

            .form-row {
                grid-template-columns: 1fr;
            }

            .footer-container {
                grid-template-columns: 1fr;
                gap: 2.5rem;
            }

            .empowering-container h2 {
                font-size: 2.2rem;
            }
        }

        @media (max-width: 480px) {
            .hero-content h1 {
                font-size: 2rem;
            }

            .service-card {
                padding: 2.5rem;
            }

            .portfolio-item {
                padding: 2.5rem;
            }
        }

        /* Scroll Reveal */
        .reveal {
            opacity: 0;
            transform: translateY(60px);
            transition: all 0.9s ease;
        }

        .reveal.active {
            opacity: 1;
            transform: translateY(0);
        }

        /* Loading Animation */
        @keyframes pulse {
            0%, 100% {
                opacity: 1;
            }
            50% {
                opacity: 0.5;
            }
        }
    </style>
</head>
<body>
    <!-- Navigation -->
    <nav id="navbar">
        <div class="nav-container">
            <div class="logo-container">
                <img src="https://i.postimg.cc/6qWFLz7m/Zinara-Digital-Marketing-1.png" alt="Zinara Digital Logo" class="logo-image">
                <a href="#" class="logo-text">ZINARA DIGITAL</a>
            </div>
            <ul class="nav-menu" id="navMenu">
                <li><a href="#home">Home</a></li>
                <li><a href="#about">About Us</a></li>
                <li><a href="#services">Services</a></li>
                <li><a href="#work">Our Work</a></li>
                <li><a href="#contact">Contact</a></li>
            </ul>
            <div class="hamburger" id="hamburger">
                <span></span>
                <span></span>
                <span></span>
            </div>
        </div>
    </nav>

    <!-- Hero Section -->
    <section class="hero" id="home">
        <div class="hero-container">
            <div class="hero-content">
                <img src="https://i.postimg.cc/6qWFLz7m/Zinara-Digital-Marketing-1.png" alt="Zinara Digital" class="hero-logo">
                <h1>Unleash Your Digital Potential</h1>
                <p class="hero-subtitle">With zinara digital marketing agency Where Visibility Meets Conversion.</p>
                <p>Zinara Digital Marketing Agency helps businesses build visibility, authority, and revenue through data driven marketing systems designed for growth that lasts.</p>
                <p class="tagline">Your Digital Growth. Simplified.</p>
                <a href="#contact" class="cta-button">Start Your Journey</a>
            </div>
            <div class="hero-image">
                <img src="https://images.unsplash.com/photo-1557804506-669a67965ba0?ixlib=rb-4.0.3&auto=format&fit=crop&w=800&q=80" alt="Digital Marketing Team">
            </div>
        </div>
    </section>

    <!-- Partner Section -->
    <section class="partner-section reveal">
        <h2>Partner with Us at Zinara Digital</h2>
        <p class="subtitle">Where strategy meets creativity to help your business thrive.</p>
        <p>At Zinara Digital, we specialize in tailored digital marketing solutions that elevate your brand visibility, attract the right audience, and turn engagement into measurable growth. Whether you're building awareness, boosting conversions, or strengthening customer relationships, our team ensures your business stands out in the moments that matter.</p>
        <p class="tagline-emphasis">Your digital growth. Simplified.</p>
        <a href="#contact" class="cta-button">Start You Journey</a>
    </section>

    <!-- Process Section -->
    <section class="process-section" id="process">
        <div class="process-container">
            <div class="process-header reveal">
                <h2>Our process clear steps to revenue</h2>
                <p>Simple, transparent, and designed to deliver results fast</p>
            </div>
            <div class="process-grid">
                <div class="process-card reveal">
                    <div class="process-number">1</div>
                    <h3>Audit & Quick Wins</h3>
                    <p>We audit your funnel and deliver 3 quick wins you can apply immediately.</p>
                    <p class="timeline">(Day 0–7)</p>
                </div>
                <div class="process-card reveal">
                    <div class="process-number">2</div>
                    <h3>Custom Growth Plan</h3>
                    <p>A tailored 30-/90-day plan with projected ROI and clear milestones.</p>
                    <p class="timeline">(Day 7)</p>
                </div>
                <div class="process-card reveal">
                    <div class="process-number">3</div>
                    <h3>Execute & Optimize</h3>
                    <p>We run campaigns, optimize weekly, and report on revenue metrics.</p>
                    <p class="timeline">(Ongoing)</p>
                </div>
            </div>
        </div>
    </section>

    <!-- About Section -->
    <section class="about-section reveal" id="about">
        <div class="about-container">
            <p class="about-label">ABOUT ZINARA DIGITAL MARKETING AGENCY</p>
            <h2>"For those who refuse to stay invisible."</h2>
            <p>Zinara helps entrepreneurs, creators, and businesses turn visibility into growth. We engineer digital ecosystems powered by strategy, storytelling, and precision designed for those who refuse to stay invisible.</p>
            <a href="#contact" class="cta-button">Start Now</a>
            
            <div class="mission-vision">
                <div class="mission-card">
                    <h3>Mission</h3>
                    <p>Amplify business stories through smart digital marketing that builds trust and drives growth.</p>
                </div>
                <div class="vision-card">
                    <h3>VISION</h3>
                    <p>Our vision is to be a leading force in digital marketing empowering brands to share their stories with authenticity, creativity, and purpose, inspiring meaningful connections that drive growth, trust, and lasting impact worldwide.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- What We Do Section -->
    <section class="what-we-do-section reveal">
        <div class="what-we-do-container">
            <h2>WHAT WE DO<br>Unleash Your Business Digital Potential</h2>
            <p>At Zinara Digital, we craft strategies that turn visibility into momentum and marketing into measurable growth. Every brand has a story we bring it to life through insight, creativity, and precision execution. By blending data with intuition, we design digital ecosystems that connect, engage, and convert. The result is clarity, consistency, and lasting impact built for brands ready to grow with purpose.</p>
            <p class="tagline-emphasis-section">Your Digital Growth. Simplified</p>
        </div>
    </section>

    <!-- Services Section -->
    <section class="services-section" id="services">
        <div class="services-container">
            <div class="services-grid">
                <!-- Service 1 -->
                <div class="service-card reveal">
                    <h3>Social Media Marketing & Management</h3>
                    <p>Your customers spend 2.5 hours daily on social media. Other businesses are capturing their attention. Are you?</p>
                    <p>We turn social platforms into revenue channels. Strategic content that stops the scroll. Community management that builds trust. Ad campaigns that target buyers at the exact moment they're ready to act. Consistent presence without the daily grind.</p>
                    <div class="what-you-get">
                        <h4>What you get:</h4>
                        <ul>
                            <li>Content that generates engagement and drives traffic to your site</li>
                            <li>Ad targeting that reaches decision-makers, not just audiences</li>
                            <li>Community management that turns followers into customers</li>
                        </ul>
                    </div>
                    <p class="service-tags">Facebook Marketing • Instagram Management • LinkedIn Strategy • Social Media Advertising • Content Creation</p>
                </div>

                <!-- Service 2 -->
                <div class="service-card reveal">
                    <h3>Virtual Assistant & Customer Management</h3>
                    <p>Acquiring a new customer costs 5x more than keeping one you already have.</p>
                    <p>Our VAs handle the daily work of keeping customers happy and coming back. Reputation monitoring. Review responses. Follow-up sequences. Social engagement. The consistent touchpoints that turn one-time buyers into loyal advocates who refer without being asked.</p>
                    <div class="what-you-get">
                        <h4>What you get:</h4>
                        <ul>
                            <li>Reputation management that drives 30%+ more inbound inquiries</li>
                            <li>Professional response systems that turn complaints into loyalty</li>
                            <li>Automated follow-ups that bring customers back on schedule</li>
                        </ul>
                    </div>
                    <p class="service-tags">Virtual Assistant Services • Online Reputation Management • Customer Relationship Management • Email Marketing • Review Management</p>
                </div>

                <!-- Service 3 -->
                <div class="service-card reveal">
                    <h3>SEO, Content Marketing & Paid Ads</h3>
                    <p>Customers can't buy from a business they can't find.</p>
                    <p>We put you in front of buyers when they're actively looking to spend. SEO that captures purchase-intent searches. Paid ads that target decision-makers, not browsers. Content that answers the questions keeping your customers up at 2am.</p>
                    <div class="what-you-get">
                        <h4>What you get:</h4>
                        <ul>
                            <li>First-page rankings for searches that actually convert</li>
                            <li>Ad campaigns returning $4.20+ for every dollar spent</li>
                            <li>Content that builds authority and drives qualified leads</li>
                        </ul>
                    </div>
                    <p class="service-tags">SEO Services • Google Ads • Meta Advertising • Content Marketing • Link Building</p>
                </div>

                <!-- Service 4 -->
                <div class="service-card reveal">
                    <h3>Marketing Automation & AI Intelligence</h3>
                    <p>While others test and guess, you'll predict and scale.</p>
                    <p>We install intelligence into your marketing engine. Predictive models spot buying signals before competitors notice. Automation handles repetitive work at machine speed. Personalization delivers the right message to the right person without manual effort.</p>
                    <div class="what-you-get">
                        <h4>What you get:</h4>
                        <ul>
                            <li>Marketing that learns and improves automatically</li>
                            <li>Behavioral insights that forecast what customers want next</li>
                            <li>Campaigns that scale without scaling your workload</li>
                        </ul>
                    </div>
                    <p class="service-tags">Marketing Automation • Predictive Analytics • AI Marketing • Customer Data Intelligence • Campaign Optimization</p>
                </div>

                <!-- Service 5 -->
                <div class="service-card reveal">
                    <h3>Web Design & Conversion Optimization</h3>
                    <p>97 out of 100 visitors leave without buying. Let's fix that.</p>
                    <p>Your website should work like a salesperson who never sleeps. We remove the confusion, eliminate the hesitation, and guide visitors smoothly from curiosity to checkout. Every click intentional. Every form field tested. Every CTA optimized.</p>
                    <div class="what-you-get">
                        <h4>What you get:</h4>
                        <ul>
                            <li>Websites built to convert, not just look pretty</li>
                            <li>Landing pages that turn 8-12% of traffic into leads</li>
                            <li>Automation that follows up so you don't have to</li>
                        </ul>
                    </div>
                    <p class="service-tags">Web Design • Conversion Rate Optimization • Landing Pages • Marketing Automation • CRM Setup</p>
                </div>
            </div>

            <div class="all-services-cta reveal">
                <p style="font-size: 1.2rem; color: var(--color-gray-accent); margin-bottom: 2rem;">Discover our comprehensive services tailored to meet your unique digital marketing needs. From SEO and content marketing to social media marketing and web design to marketing automation and AI, we have the expertise to enhance your brand's visibility and engagement.</p>
                <a href="#contact" class="cta-button">All Services</a>
            </div>
        </div>
    </section>

    <!-- Empowering Growth Section -->
    <section class="empowering-section reveal">
        <div class="empowering-container">
            <h2>Empowering Growth</h2>
            <h3>Through Data Driven Strategies</h3>
            <p>Zinara Digital empowers businesses to thrive in the digital landscape. Specializing in data driven marketing, we help companies enhance their online presence, establish industry authority, and drive sustainable revenue growth. Our tailored strategies are designed to deliver long lasting results, ensuring our clients stay ahead of the competition. With a focus on innovation and creativity, we craft campaigns that resonate with audiences and drive impactful outcomes.</p>
            <a href="#services" class="cta-button">All Services</a>
        </div>
    </section>

    <!-- Testimonials Section -->
    <section class="testimonials-section" id="testimonials">
        <div class="testimonials-container">
            <h2>What our client's say about Zinara Digital</h2>
            <p>Our clients share their success stories and experiences working with us. Discover how ZINARA DIGITAL has helped businesses achieve their digital marketing goals and grow their online presence.</p>
            
            <div class="testimonials-grid">
                <div class="testimonial-card reveal">
                    <h5>Medvera Homecare Team</h5>
                    <h4>Medvera Homecare</h4>
                    <p>Working with Zinara Digital transformed how families find our care. Their social media work, customer care, and online reputation management amplified our message, increased trust, and helped us connect with more families through prompt, compassionate support.</p>
                </div>

                <div class="testimonial-card reveal">
                    <h5>Bridget Waceke<br>Managing Director, Elan Bags</h5>
                    <h4>Elan Bags</h4>
                    <p>Zinara Digital lifted our brand visibility beyond expectations. Their content and social strategy doubled engagement and grew online sales by 250% in three months, turning our lookbook into a reliable sales channel.</p>
                </div>

                <div class="testimonial-card reveal">
                    <h5>Joseph Mwangi<br>Founder, UrbanTribe Events</h5>
                    <h4>UrbanTribe Events</h4>
                    <p>Zinara Digital sold out three major events with targeted campaigns and automation. We reduced admin time by 60% and could focus on the event experience.</p>
                </div>

                <div class="testimonial-card reveal">
                    <h5>Azma Suleiman<br>Operations Manager, GlamFix Beauty Parlour</h5>
                    <h4>GlamFix Beauty Parlour</h4>
                    <p>Zinara's digital overhaul boosted our monthly clients by 40% and streamlined bookings with automation, making daily operations much smoother.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Portfolio Section -->
    <section class="portfolio-section" id="work">
        <div class="portfolio-container">
            <div class="portfolio-header reveal">
                <h2>Our Work</h2>
                <p>Explore a showcase of our successful digital marketing campaigns and projects. Each initiative is a testament to our commitment to delivering exceptional results and driving business growth.</p>
            </div>

            <div class="portfolio-grid">
                <div class="portfolio-item reveal">
                    <h3>SEO Campaign for Tech Startup</h3>
                    <h4>Increasing Online Visibility</h4>
                    <p>Our SEO campaign for a tech startup resulted in a significant increase in organic traffic and a boost in search engine rankings. The tailored strategy focused on relevant keywords and quality content.</p>
                </div>

                <div class="portfolio-item reveal">
                    <h3>Social Media Campaign for Fashion Brand</h3>
                    <h4>Enhancing Brand Engagement</h4>
                    <p>Our social media campaign for a fashion brand led to a rise in follower engagement and an increase in website traffic. Through captivating visuals and targeted ads, we amplified brand awareness and customer interactions.</p>
                </div>

                <div class="portfolio-item reveal">
                    <h3>Content Marketing for E-Commerce Business</h3>
                    <h4>Driving Conversions Through Content</h4>
                    <p>Our content marketing efforts for an e-commerce business generated a boost in conversion rates and customer retention. By creating valuable and engaging content, we established a loyal customer base and increased sales.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Stats Section -->
    <section class="stats-section reveal">
        <div class="stats-container">
            <h2>How We Drive Measurable Growth</h2>
            <p>We're not just an agency  we're your strategic ally.</p>
            
            <div class="stats-grid">
                <div class="stat-card">
                    <div class="stat-number">50+</div>
                    <div class="stat-label">Businesses Grown</div>
                    <div class="stat-description">Companies transformed through our strategic digital solutions</div>
                </div>
                <div class="stat-card">
                    <div class="stat-number">250%</div>
                    <div class="stat-label">Average Revenue Increase</div>
                    <div class="stat-description">Typical growth achieved for our partnered clients</div>
                </div>
                <div class="stat-card">
                    <div class="stat-number">98%</div>
                    <div class="stat-label">Client Retention Rate</div>
                    <div class="stat-description">Long-term partnerships built on trust and results</div>
                </div>
            </div>

            <p style="margin-top: 4rem; font-size: 1.2rem;">We diagnose funnels, execute targeted campaigns, and run continual testing for compounding results.</p>
            
            <div class="process-flow">
                <span class="process-step">Strategy</span>
                <span class="arrow">→</span>
                <span class="process-step">Execution</span>
                <span class="arrow">→</span>
                <span class="process-step">Optimization</span>
            </div>

            <p style="margin-top: 3rem; font-size: 1.15rem; max-width: 900px; margin-left: auto; margin-right: auto;">At Zinara Digital, we engineer digital ecosystems that amplify presence, accelerate conversions, and scale retention. We combine strategy, storytelling, and automation to turn attention into revenue</p>
        </div>
    </section>

    <!-- Contact Section -->
    <section class="contact-section" id="contact">
        <div class="contact-container">
            <div class="contact-form-wrapper reveal">
                <h2>Contact Us</h2>
                <p>Get in touch by filling out the form. We'll respond as soon we can.</p>
                <form class="contact-form" id="contactForm">
                    <div class="form-row">
                        <div class="form-group">
                            <label for="firstName">First Name</label>
                            <input type="text" id="firstName" name="firstName" required>
                        </div>
                        <div class="form-group">
                            <label for="lastName">Last Name</label>
                            <input type="text" id="lastName" name="lastName" required>
                        </div>
                    </div>
                    <div class="form-group">
                        <label for="email">Email</label>
                        <input type="email" id="email" name="email" required>
                    </div>
                    <div class="form-group">
                        <label for="message">Message</label>
                        <textarea id="message" name="message" required></textarea>
                    </div>
                    <button type="submit" class="cta-button">Submit</button>
                </form>
            </div>
            <div class="contact-image reveal">
                <img src="https://i.postimg.cc/6qWFLz7m/Zinara-Digital-Marketing-1.png" alt="Zinara Digital">
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer>
        <div class="footer-container">
            <div class="footer-brand">
                <h3>ZINARA DIGITAL</h3>
                <p class="tagline">Your digital growth partner.</p>
                <p>+254704878872</p>
                <p>zinara-digital-2.com</p>
                <p>Nairobi, Kenya</p>
            </div>
            <div class="footer-section">
                <h4>Quick Links</h4>
                <a href="#home">Home</a>
                <a href="#about">About Us</a>
                <a href="#services">Services</a>
                <a href="#work">Our Work</a>
                <a href="#contact">Contact</a>
            </div>
            <div class="footer-section">
                <h4>Legal</h4>
                <a href="#">Privacy Policy</a>
                <a href="#">Accessibility Statement</a>
            </div>
            <div class="footer-section">
                <h4>Connect</h4>
                <a href="#">Facebook</a>
                <a href="#">Instagram</a>
                <a href="#">LinkedIn</a>
                <a href="#">Twitter</a>
            </div>
        </div>
        <div class="footer-bottom">
            <p>© 2025 Zinara Digital Marketing. All rights reserved.</p>
        </div>
    </footer>

    <script>
        // Navbar scroll effect
        window.addEventListener('scroll', function() {
            const navbar = document.getElementById('navbar');
            if (window.scrollY > 50) {
                navbar.classList.add('scrolled');
            } else {
                navbar.classList.remove('scrolled');
            }
        });

        // Mobile menu toggle
        const hamburger = document.getElementById('hamburger');
        const navMenu = document.getElementById('navMenu');

        hamburger.addEventListener('click', function() {
            navMenu.classList.toggle('active');
            
            const spans = hamburger.querySelectorAll('span');
            if (navMenu.classList.contains('active')) {
                spans[0].style.transform = 'rotate(45deg) translate(8px, 8px)';
                spans[1].style.opacity = '0';
                spans[2].style.transform = 'rotate(-45deg) translate(8px, -8px)';
            } else {
                spans[0].style.transform = 'none';
                spans[1].style.opacity = '1';
                spans[2].style.transform = 'none';
            }
        });

        // Close mobile menu when clicking on a link
        const navLinks = document.querySelectorAll('.nav-menu a');
        navLinks.forEach(link => {
            link.addEventListener('click', function() {
                navMenu.classList.remove('active');
                const spans = hamburger.querySelectorAll('span');
                spans[0].style.transform = 'none';
                spans[1].style.opacity = '1';
                spans[2].style.transform = 'none';
            });
        });

        // Smooth scrolling for anchor links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function(e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    const offsetTop = target.offsetTop - 70;
                    window.scrollTo({
                        top: offsetTop,
                        behavior: 'smooth'
                    });
                }
            });
        });

        // Scroll reveal animation
        function revealOnScroll() {
            const reveals = document.querySelectorAll('.reveal');
            
            reveals.forEach(element => {
                const windowHeight = window.innerHeight;
                const elementTop = element.getBoundingClientRect().top;
                const elementVisible = 120;
                
                if (elementTop < windowHeight - elementVisible) {
                    element.classList.add('active');
                }
            });
        }

        window.addEventListener('scroll', revealOnScroll);
        revealOnScroll();

        // Form submission handling
        const contactForm = document.getElementById('contactForm');
        contactForm.addEventListener('submit', function(e) {
            e.preventDefault();
            
            const formData = {
                firstName: document.getElementById('firstName').value,
                lastName: document.getElementById('lastName').value,
                email: document.getElementById('email').value,
                message: document.getElementById('message').value
            };

            alert('Thank you for your message! We will get back to you soon.');
            contactForm.reset();
        });

        // Counter animation for stats
        function animateCounter(element, target, suffix = '') {
            const duration = 2000;
            const start = 0;
            const increment = target / (duration / 16);
            let current = start;

            const timer = setInterval(() => {
                current += increment;
                if (current >= target) {
                    element.textContent = target + suffix;
                    clearInterval(timer);
                } else {
                    element.textContent = Math.floor(current) + suffix;
                }
            }, 16);
        }

        // Trigger counter animation when stats section is visible
        let statsAnimated = false;
        function checkStatsSection() {
            const statsSection = document.querySelector('.stats-section');
            if (!statsSection) return;
            
            const rect = statsSection.getBoundingClientRect();
            
            if (rect.top < window.innerHeight && rect.bottom >= 0 && !statsAnimated) {
                statsAnimated = true;
                
                const statNumbers = document.querySelectorAll('.stat-number');
                if (statNumbers.length >= 3) {
                    animateCounter(statNumbers[0], 50, '+');
                    animateCounter(statNumbers[1], 250, '%');
                    animateCounter(statNumbers[2], 98, '%');
                }
            }
        }

        window.addEventListener('scroll', checkStatsSection);
        checkStatsSection();

        // Parallax effect for hero section
        window.addEventListener('scroll', function() {
            const scrolled = window.pageYOffset;
            const hero = document.querySelector('.hero');
            if (hero && scrolled < window.innerHeight) {
                const parallaxSpeed = 0.4;
                hero.style.transform = 'translateY(' + scrolled * parallaxSpeed + 'px)';
            }
        });

        // Add interaction to service cards
        const serviceCards = document.querySelectorAll('.service-card');
        serviceCards.forEach(card => {
            card.addEventListener('mouseenter', function() {
                this.style.transition = 'all 0.4s cubic-bezier(0.4, 0, 0.2, 1)';
            });
        });

        // Lazy loading for images
        const images = document.querySelectorAll('img');
        const imageOptions = {
            threshold: 0,
            rootMargin: "0px 0px 100px 0px"
        };

        const imageObserver = new IntersectionObserver(function(entries, observer) {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    const img = entry.target;
                    img.style.opacity = '0';
                    img.style.transition = 'opacity 0.6s ease';
                    img.onload = function() {
                        img.style.opacity = '1';
                    };
                    observer.unobserve(img);
                }
            });
        }, imageOptions);

        images.forEach(img => imageObserver.observe(img));

        // Add smooth appearance for sections
        window.addEventListener('load', function() {
            setTimeout(function() {
                document.querySelectorAll('.reveal').forEach(function(element, index) {
                    setTimeout(function() {
                        if (element.getBoundingClientRect().top < window.innerHeight) {
                            element.classList.add('active');
                        }
                    }, index * 100);
                });
            }, 300);
        });

        // Add hover effects to CTA buttons
        const ctaButtons = document.querySelectorAll('.cta-button');
        ctaButtons.forEach(button => {
            button.addEventListener('mouseenter', function() {
                this.style.transform = 'translateY(-5px) scale(1.05)';
            });
            
            button.addEventListener('mouseleave', function() {
                this.style.transform = 'translateY(0) scale(1)';
            });
        });

        // Portfolio items animation
        const portfolioItems = document.querySelectorAll('.portfolio-item');
        portfolioItems.forEach(item => {
            item.addEventListener('mouseenter', function() {
                this.style.borderLeft = '6px solid var(--color-aqua)';
            });
            
            item.addEventListener('mouseleave', function() {
                this.style.borderLeft = 'none';
            });
        });

        // Testimonial cards enhanced interaction
        const testimonialCards = document.querySelectorAll('.testimonial-card');
        testimonialCards.forEach(card => {
            card.addEventListener('mouseenter', function() {
                this.style.background = 'var(--color-blush)';
            });
            
            card.addEventListener('mouseleave', function() {
                this.style.background = 'var(--color-light)';
            });
        });

        // Process cards sequenced animation on scroll
        let processCardsAnimated = false;
        function animateProcessCards() {
            if (processCardsAnimated) return;
            
            const processSection = document.querySelector('.process-section');
            if (!processSection) return;
            
            const rect = processSection.getBoundingClientRect();
            if (rect.top < window.innerHeight * 0.75) {
                processCardsAnimated = true;
                const cards = document.querySelectorAll('.process-card');
                cards.forEach((card, index) => {
                    setTimeout(() => {
                        card.style.animation = 'fadeInUp 0.8s ease forwards';
                    }, index * 200);
                });
            }
        }

        window.addEventListener('scroll', animateProcessCards);

        // Header shrink on scroll
        let lastScroll = 0;
        window.addEventListener('scroll', function() {
            const currentScroll = window.pageYOffset;
            const navbar = document.getElementById('navbar');
            
            if (currentScroll > lastScroll && currentScroll > 100) {
                navbar.style.transform = 'translateY(-100%)';
            } else {
                navbar.style.transform = 'translateY(0)';
            }
            lastScroll = currentScroll;
        });

        // Add dynamic background to hero on mouse move
        const hero = document.querySelector('.hero');
        if (hero) {
            hero.addEventListener('mousemove', function(e) {
                const x = e.clientX / window.innerWidth;
                const y = e.clientY / window.innerHeight;
                
                this.style.background = `linear-gradient(${135 + x * 20}deg, var(--color-blush) 0%, var(--color-white) 100%)`;
            });
        }

        // Form validation with visual feedback
        const formInputs = document.querySelectorAll('.form-group input, .form-group textarea');
        formInputs.forEach(input => {
            input.addEventListener('blur', function() {
                if (this.value.trim() !== '') {
                    this.style.borderColor = 'var(--color-aqua)';
                } else if (this.hasAttribute('required')) {
                    this.style.borderColor = 'var(--color-orange)';
                }
            });
            
            input.addEventListener('focus', function() {
                this.style.borderColor = 'var(--color-orange)';
            });
        });

        // Stats section background animation
        const statsSection = document.querySelector('.stats-section');
        if (statsSection) {
            let statsHue = 0;
            setInterval(() => {
                statsHue = (statsHue + 0.1) % 360;
                const lightness = 20 + Math.sin(statsHue / 60) * 5;
            }, 50);
        }

        // Scroll to top button (hidden by default, appears on scroll)
        const scrollTopBtn = document.createElement('button');
        scrollTopBtn.innerHTML = '↑';
        scrollTopBtn.style.cssText = `
            position: fixed;
            bottom: 30px;
            right: 30px;
            width: 50px;
            height: 50px;
            background: var(--color-orange);
            color: white;
            border: none;
            border-radius: 50%;
            font-size: 24px;
            cursor: pointer;
            opacity: 0;
            transition: all 0.3s ease;
            z-index: 999;
            box-shadow: 0 4px 15px rgba(255, 107, 53, 0.3);
        `;
        document.body.appendChild(scrollTopBtn);

        window.addEventListener('scroll', function() {
            if (window.pageYOffset > 500) {
                scrollTopBtn.style.opacity = '1';
                scrollTopBtn.style.transform = 'translateY(0)';
            } else {
                scrollTopBtn.style.opacity = '0';
                scrollTopBtn.style.transform = 'translateY(20px)';
            }
        });

        scrollTopBtn.addEventListener('click', function() {
            window.scrollTo({
                top: 0,
                behavior: 'smooth'
            });
        });

        scrollTopBtn.addEventListener('mouseenter', function() {
            this.style.background = 'var(--color-sea-blue)';
            this.style.transform = 'scale(1.1)';
        });

        scrollTopBtn.addEventListener('mouseleave', function() {
            this.style.background = 'var(--color-orange)';
            this.style.transform = 'scale(1)';
        });

        // Performance optimization - debounce scroll events
        function debounce(func, wait) {
            let timeout;
            return function executedFunction(...args) {
                const later = () => {
                    clearTimeout(timeout);
                    func(...args);
                };
                clearTimeout(timeout);
                timeout = setTimeout(later, wait);
            };
        }

        const debouncedReveal = debounce(revealOnScroll, 10);
        const debouncedStats = debounce(checkStatsSection, 10);
        
        window.addEventListener('scroll', debouncedReveal);
        window.addEventListener('scroll', debouncedStats);

        // Add entrance animation to logo
        window.addEventListener('load', function() {
            const logoImage = document.querySelector('.logo-image');
            if (logoImage) {
                logoImage.style.animation = 'fadeInUp 1s ease';
            }
        });

        // Console message for developers
        console.log('%c👋 Built by Zinara Digital', 'color: #FF6B35; font-size: 20px; font-weight: bold;');
        console.log('%cYour Digital Growth. Simplified.', 'color: #03A6A1; font-size: 14px; font-style: italic;');
        console.log('%cInterested in our services? Contact us at zinara-digital-2.com', 'color: #006B7D; font-size: 12px;');
    </script>
</body>
</html>
